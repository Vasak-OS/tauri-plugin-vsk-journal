/.vs
.DS_Store
.Thumbs.db
*.sublime*
.idea/
debug.log
package-lock.json
.vscode/settings.json
yarn.lock

/.tauri
/target

# Cargo.lock is intentionally tracked: the plugin is compiled into VasakOS apps,
# so pinning transitive dependency versions keeps builds reproducible (Sonar text:S8570).
node_modules/

dist-js
dist

examples/